
services:
  db:
    image: postgres:16
    container_name: swim_db
    environment:
      POSTGRES_DB: swim_competition
      POSTGRES_USER: swim_user
      POSTGRES_PASSWORD: swim_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: